# [2026-06-28 21:11] Adopt 2030ai project template

Файл: `agent_docs/adr/2026-06-28-2111-adopt-2030ai-template.md`

## Статус

Принято

## Контекст

Проект уже содержит инженерные документы в `docs/`, но не содержит project-local agent rules, атомарные журналы решений/итераций и единые docs-гайды.

Пользователь попросил применить `2030ai/2030ai-project-template`.

## Решение

Применить шаблон как процессный слой:

- добавить `AGENTS.md`, `CLAUDE.md`, agent skill directories;
- добавить `agent_docs/` с guides/templates/ADR/development-history;
- сохранить текущие `docs/` как canonical инженерные runbooks;
- не заменять текущий `README.md` шаблонным README;
- удалить одноразовый `setup-checklist.md` и template-source history entries;
- адаптировать `.gitignore`, `.editorconfig`, `.env.example`, `.cursorignore`, markdownlint CI.

## Обоснование

Так проект получает единые правила для agent work без потери уже созданной stage-1 документации и без копирования template-specific истории.

## Последствия

Положительные:

- появляются атомарные ADR и development history;
- агентам проще восстанавливать контекст;
- GitHub публикация получает базовый markdownlint.

Отрицательные:

- repo становится более документным;
- markdownlint может потребовать подправить старые markdown-файлы перед CI.

## Связанные записи

- `agent_docs/development-history/2026-06-28-2111-apply-2030ai-template.md`

## Альтернативы рассмотрены

- Скопировать шаблон целиком поверх repo: отклонено, потерялся бы текущий `README.md` и появились бы template-source записи.
- Оставить только `AGENTS.md`: отклонено, потому что пользователь просил применить шаблон, а не один файл.
