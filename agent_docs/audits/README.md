# Audits

Проверки соответствия, ревью и отчёты.

Правила:

- один аудит — один файл;
- имя файла: `YYYY-MM-DD-HHMM-short-title.md`;
- verdict использовать явно: `PASS`, `WARN`, `CRITICAL_FAIL`, `UNKNOWN`;
- `UNKNOWN` не считать `PASS`.
